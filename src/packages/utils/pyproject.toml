[tool.poetry]
name = "utils"
version = "0.2.0"
description = "Python package that gets the current datetime."
authors = ["Your Name <you@example.com>"]
readme = "README.md"

packages = [
    { include = "utils" }
]

[tool.poetry.scripts]
main = "utils.main:add_processing_timestamp"

[tool.poetry.dependencies]
python = "^3.10"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
